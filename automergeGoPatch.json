{
  "packageRules": [
    {
      "matchUpdateTypes": ["patch"],
      "matchDepTypes": ["golang", "toolchain"],
      "matchPackagePatterns": [
        "^golang$",
        "^go$"
      ],
      "automerge": true
    }
  ]
}
